---
title: "STM Analysis"
output: html_document
---

```{r}
# This is a code chunk
library(stm)
data <- read.csv("data.csv")
4. You can also add markdown text between the code chunks to explain what the code does. For example:
```